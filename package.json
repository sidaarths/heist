{
  "name": "heist",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun run --cwd packages/server src/index.ts & bun run --cwd packages/client dev",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  }
}
